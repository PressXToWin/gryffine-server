server {
  listen 80;
  index index.html;

  location /static/ {
    alias /usr/share/nginx/html/;
    index index.html;
  }

  location / {
    proxy_set_header Host $http_host;
    proxy_pass http://backend:8000/;
  }

}